Payment lifecycle, webhook idempotency, and adapter contract tests are executed from `backend/tests/`.

The source tree keeps this folder so the simulator package layout documents the intended enterprise test areas without compiling Vitest files into the production build.

