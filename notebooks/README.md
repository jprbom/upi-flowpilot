# Notebook Guide

This project keeps notebook work as documented analysis recipes because the local portfolio build uses Node and TypeScript. The synthetic data files in data can be imported into Python, R, DuckDB, or Observable notebooks for EDA, feature exploration, and model benchmarking.

